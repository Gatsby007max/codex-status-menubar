# Fixtures

These fixtures are synthetic `CODEX_HOME` directories used by `scripts/verify.sh`.

They must not contain real prompts, responses, credentials, private local paths, or copied
Codex session logs.

